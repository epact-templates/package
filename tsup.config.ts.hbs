import { defineConfig } from "tsup"

export default defineConfig({
  entry: ["src/index.ts"],
  clean: true,
  splitting: true,
  dts: true,
  format: ["cjs", "esm"{{#if_ne mode "node"}}, "iife"}],
  target: {{#if_eq mode "node"}}"node16"{{/if_eq}}{{#if_ne mode "node"}}"es2015"{{/if_ne}}
})